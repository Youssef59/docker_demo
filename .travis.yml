language: java
services:
    - docker
jdk:
  - openjdk11

script:
  - ./mvnw clean install
  - ./mvnw deploy jib:build -P deploy-docker
  - docker build -f Dockerfile -t hellomagyar/method2 .
  - ./mvnw heroku:deploy jib:build -P deploy-heroku,deploy-docker

